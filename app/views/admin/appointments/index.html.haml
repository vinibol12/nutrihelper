%p#notice= notice
%h5
  %strong Appointments History for #{@user.full_name}
%table.table.striped
  %thead
    %tr.row
      %th When Happened
      %th Notes

  %tbody  
    - @appointments.each do |appointment|
      - evaluation = appointment.anthropometric_evaluation
      %tr.row
        %td= appointment.date_time
        %td= appointment.notes
        %td= link_to 'Go To Evaluation', admin_anthropometric_evaluation_path(evaluation) if evaluation
        %td
          = button_to 'Delete', admin_appointment_path(appointment), method: :delete, class: 'btn red darken-2', data: { confirm: 'Are you sure? This will delete any Evaluations made during this appointment'}

-#= button_to 'Create Appointment for Client', admin_appointments_path, class: 'btn btn-primary', params:{ appointment: {user_id: @user.id }}
